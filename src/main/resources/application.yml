spring:
  application:
    name: service-tenant
  config:
    enabled: true
    import: configserver:https://service-config.azurewebsites.net
  cloud:
    config:
      username: ${EUR_USER}
      password: ${EUR_PASS}
    azure:
      active-directory:
        enabled: true
        credential:
          client-id: 54ebc1d3-3015-446b-9187-37c6a7b5018c
          app-id-uri: api://54ebc1d3-3015-446b-9187-37c6a7b5018c
settings:
  cors_origin: http://localhost:4200/
